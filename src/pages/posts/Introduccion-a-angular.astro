---
import PostsLayout from "../../layouts/PostsLayout.astro";

export const frontmatterAngular = {
  image_author: "../public/assets/UserImages/Nicobyte.jpg",
  img: "../../assets/frameworks/angular.png",
  title: "Introduccion a Angular",
  date: "March 5, 2025",
  topic: "Learning",
  topic01: "Astro",
  topic02: "Dessarrollo",
  author: "Sofia Fernandez",
};
---

<PostsLayout
  image_author={frontmatterAngular.image_author}
  img={frontmatterAngular.img}
  title={frontmatterAngular.title}
  date={frontmatterAngular.date}
  topic={frontmatterAngular.topic}
  topic01={frontmatterAngular.topic01}
  topic02={frontmatterAngular.topic02}
  author={frontmatterAngular.author}
>
  <p class="mb-4">
    Angular es un framework de desarrollo web basado en TypesScript que permite
    crear aplicaciones dinamicas y robustas para la web. Con enfoque en la
    escalabilidad, mantenibilidad y el desarrollo modular, Angular es una de las
    herramientas mas populares para crear aplicaciones modernas de una sola
    pagina (SPA)
  </p>

  <h2 class="text-2xl font-semibold mb-3">¿Por qué Elegir Angular?</h2>
  <p class="mb-4">
    Angular ofrece un conjunto completo de herramientas y características para
    construir aplicaciones web complejas y escalables. Su arquitectura basada en
    componentes y su sistema de inyección de dependencias facilitan la creación
    y gestión de aplicaciones grandes y dinámicas.
  </p>

  <h2 class="text-2xl font-semibold mb-3">Características Principales</h2>
  <ul class="list-disc pl-6 space-y-3">
    <li>
      <strong class="font-medium">Arquitectura Basada en Componentes</strong>:
      Angular organiza las aplicaciones en módulos y componentes, lo que permite
      un desarrollo más estructurado y reutilizable.
      <pre
        class="bg-gray-800 text-gray-100 p-4 rounded-md overflow-x-auto">
{`// Ejemplo básico de componente en Angular
import { Component } from '@angular/core';

@Component({
  selector: 'app-saludo',
  template: '<h1>¡Hola desde Angular!</h1>',
})
export class SaludoComponent {}
`}
    </pre>
    </li>
    <li>
      <strong class="font-medium">Sistema de Inyección de Dependencias</strong>:
      Angular cuenta con un sistema de inyección de dependencias que facilita la
      gestión de servicios y datos compartidos entre componentes.
      <pre
        class="bg-gray-800 text-gray-100 p-4 rounded-md overflow-x-auto">
{`// Servicio básico en Angular
import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class SaludoService {
  getMensaje() {
    return '¡Hola desde el servicio de Angular!';
  }
}`}
    </pre>
    </li>
    <li>
      <strong class="font-medium">Ruteo y Navegación</strong>: Angular incluye
      un potente sistema de ruteo que permite crear aplicaciones SPA con
      navegación fluida y controlada.
      <pre
        class="bg-gray-800 text-gray-100 p-4 rounded-md overflow-x-auto">
{`// Configuración de rutas en Angular
import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';
import { HomeComponent } from './home/home.component';

const routes: Routes = [
  { path: '', component: HomeComponent },
  { path: 'about', component: AboutComponent },
];

@NgModule({
  imports: [RouterModule.forRoot(routes)],
  exports: [RouterModule],
})
export class AppRoutingModule {}
`}
    </pre>
    </li>
  </ul>
</PostsLayout>
